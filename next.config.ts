import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configurações específicas (imagens do Supabase Storage, headers do
  // Service Worker etc.) entram nas próximas etapas, conforme forem
  // necessárias — mantido mínimo na Etapa 1 de propósito.
};

export default nextConfig;
